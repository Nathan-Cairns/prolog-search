/* Assume we have the fact predicates ChildOf(child, parent), Male(person), Female(person) */

 /* motherOf/2 def */
 MotherOf(mother, person)
 
 /* sisterOf/2 def */
 SisterOf(sister, person)
 
 /* inSameTree/2 def */
 inSameTree(node1, node2)